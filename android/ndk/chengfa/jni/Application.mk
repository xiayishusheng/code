# 需要被构建的模块
#APP_MODULES := demo chengfa

# 优化级别release(默认)、debug
#APP_OPTIM := release

# 编译C 和 C++源文件，传递给编译器
#APP_CFLAGS :=

# 编译C++源文件，传递给编译器
#APP_CPPFLAGS :=

# 目标平台
#APP_PLATFORM = android-21

# NDK默认寻找jni子目录下的Android.mk，该变量可以自定义文件名
#APP_BUILD_SCRIPT = MyAndroid.mk

# NDK默认构建armeabi，该变量可以生成其他架构的文件，设置为all可以生成全部平台二进制文件
#APP_ABI := armeabi mips

# NDK默认使用最小STL运行库，可以用该变量选择不同的STL实现
#APP_STL := stlport_shared

