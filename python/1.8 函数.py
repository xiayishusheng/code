# -*- coding: cp936 -*-
# 1.8 函数

print "2**3 = %d" % 2**3
print "pow(2, 3) = %d" % pow(2, 3)

print "10 + pow(2, 3*5) / 3.0 = %f" % ( 10 + pow(2, 3*5) / 3.0 )

print "abs(-10) = %d" % abs(-10)

# round()函数会把浮点数四舍五入为最接近的整数值
print "round(1.0 / 2.0) = %f" % round(1.0 / 2.0)
